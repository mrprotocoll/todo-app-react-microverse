import Header from 'component/Header';
import TodoContainer from 'component/TodoContainer';

const Home = () => (
  <div className="">
    <Header />
    <TodoContainer />
  </div>
);
export default Home;
